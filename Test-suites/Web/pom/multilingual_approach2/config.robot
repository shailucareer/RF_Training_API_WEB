*** Variables ***
${language}       en
&{LOCATORS}

# How we are going to get/fetch/read the data from dict
# ${LOCATORS['SelectLanguage_dropdown']} >> id:language-selector-button
# ${LOCATORS['Products_link']} >> xpath:(//button[contains(.,'Products')])[1]

# How we are going to store the data in dict
# 1
# KEY >> SelectLanguage_dropdown
# VALUE >> id:language-selector-button

# 2
# KEY >> Products_link
# VALUE >> xpath:(//button[contains(.,'Products')])[1]

# nth
# KEY
# VALUE